# bespin
v1 / MVP of Elyron

## Overview
This is the initial version and Minimum Viable Product (MVP) of the Elyron AI system.

## Development
This project uses pre-commit hooks for code quality. To set up:
```bash
pip install pre-commit
pre-commit install
```
